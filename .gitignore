# Dependencies
node_modules/
**/node_modules/

# Build outputs
dist/
build/
**/dist/
**/build/
*.tsbuildinfo
mavino-1.*
# Env / secrets
.env
.env.local
.env.*.local
server/.env
client/.env
.seed-credentials.txt

# Database & uploads (runtime data, not committed)
server/data/
server/uploads/
*.db
*.db-journal
*.sqlite
*.sqlite-journal

# Backups (created by deploy/backup.sh)
backups/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Vite
client/.vite/

# Prisma generated client (committed in schema dir is fine, but not root artifacts)
server/prisma/migrations/dev.db*

# Misc
.cache/
coverage/

# Android release signing keystore — NEVER commit this.
# Back it up somewhere safe outside the repo (password manager, encrypted backup).
*.keystore
*.jks
*.keystore.b64
*.jks.b64
